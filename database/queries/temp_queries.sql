-- Use these queries in the code for milestone 0

FROM users SELECT * FROM users;

FROM users SELECT * FROM users WHERE favorite_color = 'red';

FROM users SELECT * FROM users WHERE favorite_color = 'red' ORDER BY name;